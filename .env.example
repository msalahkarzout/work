# Backend Environment Variables
# Set these in your deployment platform (Render, Railway, etc.)

# Server port (Render/Railway will set this automatically)
PORT=8080

# Database connection (use your Neon or Supabase PostgreSQL URL)
DATABASE_URL=jdbc:postgresql://localhost:5432/business_db
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=your_password_here

# JWT Secret (use a strong, unique secret in production)
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random

# CORS Origins (your frontend URL)
CORS_ORIGINS=http://localhost:3000
# For production: CORS_ORIGINS=https://your-app.vercel.app
